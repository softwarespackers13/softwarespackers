# Icon Assets

Place custom icon files (SVG) in this directory if needed beyond Lucide React icons.

The application primarily uses Lucide React for icons, but you can add custom SVGs here for brand-specific or unique icons.
